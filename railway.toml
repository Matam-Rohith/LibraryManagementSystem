[build]
builder = "NIXPACKS"
buildCommand = "dotnet publish -c Release -o ./publish"

[deploy]
startCommand = "dotnet ./publish/LibraryManagementSystem.dll"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
